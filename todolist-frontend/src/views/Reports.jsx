import React from 'react';
import { Box, Typography } from '@mui/material';

const Reports = () => (
  <Box sx={{ p: 4 }}>
    <Typography variant="h4" gutterBottom>
      Raporty
    </Typography>
    <Typography>
      Tu możesz wyświetlać różne raporty i statystyki.
    </Typography>
  </Box>
);

export default Reports;